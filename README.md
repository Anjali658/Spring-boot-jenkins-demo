# Spring Boot + MySQL CRUD Project

## Setup
1. Create MySQL DB:
   CREATE DATABASE testdb;

2. Update username/password in application.properties

3. Run project:
   mvn spring-boot:run

## APIs

POST   /users
GET    /users
GET    /users/{id}
DELETE /users/{id}
